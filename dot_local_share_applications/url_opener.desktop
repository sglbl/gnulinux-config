# This desktop entry opens Windows .url files
# in the default browser automatically
[Desktop Entry]
Type=Application
Name=URL Opener
Icon=/home/sglbl/.local/share/applications/icons/url.png

# Exec breakdown:
# bash -c '…'       → run a bash command
# sed -n "s/^URL=//p" "$1" → extract the URL from the .url file passed as $1
# xdg-open "$url"    → open the extracted URL in the default browser
# _ %f               → _ is $0 placeholder, %f is the filename of the .url file
Exec=bash -c 'xdg-open "$(sed -n "s/^URL=//p" "$1")"' _ %f

MimeType=application/x-mswinurl;
NoDisplay=false
Terminal=false
Categories=Utility;
