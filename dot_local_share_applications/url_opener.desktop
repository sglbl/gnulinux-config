# This desktop entry opens Windows .url files
# in the default browser automatically
[Desktop Entry]
Type=Application
Name=URL Opener
Icon=/home/sglbl/.local/share/applications/icons/url.png

# Exec breakdown:
# bash -c '…'       → run a bash command
# grep/sed/tr       → extract URL handling both .url format and plain text URLs
# xdg-open "$url"   → open the extracted URL in the default browser
# _ %f              → _ is $0 placeholder, %f is the filename of the input file
Exec=bash -c 'xdg-open "$(grep -Eom1 "^URL=.*|^https?://.*" "$1" | sed "s/^URL=//" | tr -d "\r")"' _ %f

MimeType=application/x-mswinurl;
NoDisplay=false
Terminal=false
Categories=Utility;
