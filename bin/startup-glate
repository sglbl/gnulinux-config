#!/bin/bash
/snap/bin/glate &
while true; do
    sleep 1
    w=$(xdotool search --onlyvisible --name "Glate")
    if [ ! -z "$w" ]; then
        echo "minimizing $w"
        xdotool windowminimize $w
        break
    fi
done
