#!/bin/bash
signal-desktop %U &
while true; do
    sleep 1
    w=$(xdotool search --onlyvisible --name "Signal")
    if [ ! -z "$w" ]; then
        echo "minimizing $w"
        xdotool windowminimize $w
        break
    fi
done
